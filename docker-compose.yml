version: "3.8"

services:
  harborbucks:
    build: .
    container_name: harborbucks
    restart: unless-stopped
    ports:
      - "8099:3000"
    volumes:
      - /opt/appdata/harborbucks:/data
    environment:
      - DB_PATH=/data/harborbucks.db
      - PORT=3000
      # Set to "true" if your reverse proxy terminates HTTPS
      - SECURE_COOKIES=false
